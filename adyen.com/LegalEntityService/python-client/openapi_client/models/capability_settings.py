# coding: utf-8

"""
    Legal Entity Management API

    The Legal Entity Management API enables you to manage legal entities that contain information required for verification.  ## Authentication To connect to the Legal Entity Management API, you must use the basic authentication credentials of your web service user. If you don't have one, contact the [Adyen Support Team](https://www.adyen.help/hc/en-us/requests/new). Use the web service user credentials to authenticate your request, for example:  ``` curl -U \"ws_123456@Scope.BalancePlatform_YourBalancePlatform\":\"YourWsPassword\" \\ -H \"Content-Type: application/json\" \\ ... ``` Note that when going live, you need to generate new web service user credentials to access the [live endpoints](https://docs.adyen.com/development-resources/live-endpoints).  ## Versioning The Legal Entity Management API supports versioning of its endpoints through a version suffix in the endpoint URL. This suffix has the following format: \"vXX\", where XX is the version number.  For example: ``` https://kyc-test.adyen.com/lem/v3/legalEntities ``` ## Going live When going live, your Adyen contact will provide your API credential for the live environment. You can then use the username and password to send requests to `https://kyc-live.adyen.com/lem/v3`.  

    The version of the OpenAPI document: 3
    Contact: developer-experience@adyen.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from openapi_client.models.amount import Amount
from typing import Optional, Set
from typing_extensions import Self

class CapabilitySettings(BaseModel):
    """
    CapabilitySettings
    """ # noqa: E501
    amount_per_industry: Optional[Dict[str, Amount]] = Field(default=None, description="The maximum amount a card holder can spend per industry.", alias="amountPerIndustry")
    authorized_card_users: Optional[StrictBool] = Field(default=None, description="The number of card holders who can use the card.", alias="authorizedCardUsers")
    funding_source: Optional[List[StrictStr]] = Field(default=None, description="The funding source of the card, for example **debit**.", alias="fundingSource")
    interval: Optional[StrictStr] = Field(default=None, description="The period when the rule conditions apply.")
    max_amount: Optional[Amount] = Field(default=None, description="The maximum amount a card holder can withdraw per day.", alias="maxAmount")
    __properties: ClassVar[List[str]] = ["amountPerIndustry", "authorizedCardUsers", "fundingSource", "interval", "maxAmount"]

    @field_validator('funding_source')
    def funding_source_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        for i in value:
            if i not in set(['credit', 'debit', 'prepaid']):
                raise ValueError("each list item must be one of ('credit', 'debit', 'prepaid')")
        return value

    @field_validator('interval')
    def interval_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['daily', 'monthly', 'weekly']):
            raise ValueError("must be one of enum values ('daily', 'monthly', 'weekly')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CapabilitySettings from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each value in amount_per_industry (dict)
        _field_dict = {}
        if self.amount_per_industry:
            for _key_amount_per_industry in self.amount_per_industry:
                if self.amount_per_industry[_key_amount_per_industry]:
                    _field_dict[_key_amount_per_industry] = self.amount_per_industry[_key_amount_per_industry].to_dict()
            _dict['amountPerIndustry'] = _field_dict
        # override the default output from pydantic by calling `to_dict()` of max_amount
        if self.max_amount:
            _dict['maxAmount'] = self.max_amount.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CapabilitySettings from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "amountPerIndustry": dict(
                (_k, Amount.from_dict(_v))
                for _k, _v in obj["amountPerIndustry"].items()
            )
            if obj.get("amountPerIndustry") is not None
            else None,
            "authorizedCardUsers": obj.get("authorizedCardUsers"),
            "fundingSource": obj.get("fundingSource"),
            "interval": obj.get("interval"),
            "maxAmount": Amount.from_dict(obj["maxAmount"]) if obj.get("maxAmount") is not None else None
        })
        return _obj


