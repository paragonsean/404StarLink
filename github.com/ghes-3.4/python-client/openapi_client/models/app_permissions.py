# coding: utf-8

"""
    GitHub v3 REST API

    GitHub's v3 REST API.

    The version of the OpenAPI document: 1.1.4
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class AppPermissions(BaseModel):
    """
    The permissions granted to the user-to-server access token.
    """ # noqa: E501
    actions: Optional[StrictStr] = Field(default=None, description="The level of permission to grant the access token for GitHub Actions workflows, workflow runs, and artifacts.")
    administration: Optional[StrictStr] = Field(default=None, description="The level of permission to grant the access token for repository creation, deletion, settings, teams, and collaborators creation.")
    checks: Optional[StrictStr] = Field(default=None, description="The level of permission to grant the access token for checks on code.")
    contents: Optional[StrictStr] = Field(default=None, description="The level of permission to grant the access token for repository contents, commits, branches, downloads, releases, and merges.")
    deployments: Optional[StrictStr] = Field(default=None, description="The level of permission to grant the access token for deployments and deployment statuses.")
    environments: Optional[StrictStr] = Field(default=None, description="The level of permission to grant the access token for managing repository environments.")
    issues: Optional[StrictStr] = Field(default=None, description="The level of permission to grant the access token for issues and related comments, assignees, labels, and milestones.")
    members: Optional[StrictStr] = Field(default=None, description="The level of permission to grant the access token for organization teams and members.")
    metadata: Optional[StrictStr] = Field(default=None, description="The level of permission to grant the access token to search repositories, list collaborators, and access repository metadata.")
    organization_administration: Optional[StrictStr] = Field(default=None, description="The level of permission to grant the access token to manage access to an organization.")
    organization_hooks: Optional[StrictStr] = Field(default=None, description="The level of permission to grant the access token to manage the post-receive hooks for an organization.")
    organization_packages: Optional[StrictStr] = Field(default=None, description="The level of permission to grant the access token for organization packages published to GitHub Packages.")
    organization_plan: Optional[StrictStr] = Field(default=None, description="The level of permission to grant the access token for viewing an organization's plan.")
    organization_projects: Optional[StrictStr] = Field(default=None, description="The level of permission to grant the access token to manage organization projects and projects beta (where available).")
    organization_secrets: Optional[StrictStr] = Field(default=None, description="The level of permission to grant the access token to manage organization secrets.")
    organization_self_hosted_runners: Optional[StrictStr] = Field(default=None, description="The level of permission to grant the access token to view and manage GitHub Actions self-hosted runners available to an organization.")
    organization_user_blocking: Optional[StrictStr] = Field(default=None, description="The level of permission to grant the access token to view and manage users blocked by the organization.")
    packages: Optional[StrictStr] = Field(default=None, description="The level of permission to grant the access token for packages published to GitHub Packages.")
    pages: Optional[StrictStr] = Field(default=None, description="The level of permission to grant the access token to retrieve Pages statuses, configuration, and builds, as well as create new builds.")
    pull_requests: Optional[StrictStr] = Field(default=None, description="The level of permission to grant the access token for pull requests and related comments, assignees, labels, milestones, and merges.")
    repository_hooks: Optional[StrictStr] = Field(default=None, description="The level of permission to grant the access token to manage the post-receive hooks for a repository.")
    repository_projects: Optional[StrictStr] = Field(default=None, description="The level of permission to grant the access token to manage repository projects, columns, and cards.")
    secret_scanning_alerts: Optional[StrictStr] = Field(default=None, description="The level of permission to grant the access token to view and manage secret scanning alerts.")
    secrets: Optional[StrictStr] = Field(default=None, description="The level of permission to grant the access token to manage repository secrets.")
    security_events: Optional[StrictStr] = Field(default=None, description="The level of permission to grant the access token to view and manage security events like code scanning alerts.")
    single_file: Optional[StrictStr] = Field(default=None, description="The level of permission to grant the access token to manage just a single file.")
    statuses: Optional[StrictStr] = Field(default=None, description="The level of permission to grant the access token for commit statuses.")
    team_discussions: Optional[StrictStr] = Field(default=None, description="The level of permission to grant the access token to manage team discussions and related comments.")
    vulnerability_alerts: Optional[StrictStr] = Field(default=None, description="The level of permission to grant the access token to manage Dependabot alerts.")
    workflows: Optional[StrictStr] = Field(default=None, description="The level of permission to grant the access token to update GitHub Actions workflow files.")
    __properties: ClassVar[List[str]] = ["actions", "administration", "checks", "contents", "deployments", "environments", "issues", "members", "metadata", "organization_administration", "organization_hooks", "organization_packages", "organization_plan", "organization_projects", "organization_secrets", "organization_self_hosted_runners", "organization_user_blocking", "packages", "pages", "pull_requests", "repository_hooks", "repository_projects", "secret_scanning_alerts", "secrets", "security_events", "single_file", "statuses", "team_discussions", "vulnerability_alerts", "workflows"]

    @field_validator('actions')
    def actions_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['read', 'write']):
            raise ValueError("must be one of enum values ('read', 'write')")
        return value

    @field_validator('administration')
    def administration_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['read', 'write']):
            raise ValueError("must be one of enum values ('read', 'write')")
        return value

    @field_validator('checks')
    def checks_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['read', 'write']):
            raise ValueError("must be one of enum values ('read', 'write')")
        return value

    @field_validator('contents')
    def contents_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['read', 'write']):
            raise ValueError("must be one of enum values ('read', 'write')")
        return value

    @field_validator('deployments')
    def deployments_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['read', 'write']):
            raise ValueError("must be one of enum values ('read', 'write')")
        return value

    @field_validator('environments')
    def environments_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['read', 'write']):
            raise ValueError("must be one of enum values ('read', 'write')")
        return value

    @field_validator('issues')
    def issues_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['read', 'write']):
            raise ValueError("must be one of enum values ('read', 'write')")
        return value

    @field_validator('members')
    def members_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['read', 'write']):
            raise ValueError("must be one of enum values ('read', 'write')")
        return value

    @field_validator('metadata')
    def metadata_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['read', 'write']):
            raise ValueError("must be one of enum values ('read', 'write')")
        return value

    @field_validator('organization_administration')
    def organization_administration_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['read', 'write']):
            raise ValueError("must be one of enum values ('read', 'write')")
        return value

    @field_validator('organization_hooks')
    def organization_hooks_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['read', 'write']):
            raise ValueError("must be one of enum values ('read', 'write')")
        return value

    @field_validator('organization_packages')
    def organization_packages_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['read', 'write']):
            raise ValueError("must be one of enum values ('read', 'write')")
        return value

    @field_validator('organization_plan')
    def organization_plan_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['read']):
            raise ValueError("must be one of enum values ('read')")
        return value

    @field_validator('organization_projects')
    def organization_projects_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['read', 'write', 'admin']):
            raise ValueError("must be one of enum values ('read', 'write', 'admin')")
        return value

    @field_validator('organization_secrets')
    def organization_secrets_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['read', 'write']):
            raise ValueError("must be one of enum values ('read', 'write')")
        return value

    @field_validator('organization_self_hosted_runners')
    def organization_self_hosted_runners_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['read', 'write']):
            raise ValueError("must be one of enum values ('read', 'write')")
        return value

    @field_validator('organization_user_blocking')
    def organization_user_blocking_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['read', 'write']):
            raise ValueError("must be one of enum values ('read', 'write')")
        return value

    @field_validator('packages')
    def packages_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['read', 'write']):
            raise ValueError("must be one of enum values ('read', 'write')")
        return value

    @field_validator('pages')
    def pages_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['read', 'write']):
            raise ValueError("must be one of enum values ('read', 'write')")
        return value

    @field_validator('pull_requests')
    def pull_requests_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['read', 'write']):
            raise ValueError("must be one of enum values ('read', 'write')")
        return value

    @field_validator('repository_hooks')
    def repository_hooks_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['read', 'write']):
            raise ValueError("must be one of enum values ('read', 'write')")
        return value

    @field_validator('repository_projects')
    def repository_projects_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['read', 'write', 'admin']):
            raise ValueError("must be one of enum values ('read', 'write', 'admin')")
        return value

    @field_validator('secret_scanning_alerts')
    def secret_scanning_alerts_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['read', 'write']):
            raise ValueError("must be one of enum values ('read', 'write')")
        return value

    @field_validator('secrets')
    def secrets_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['read', 'write']):
            raise ValueError("must be one of enum values ('read', 'write')")
        return value

    @field_validator('security_events')
    def security_events_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['read', 'write']):
            raise ValueError("must be one of enum values ('read', 'write')")
        return value

    @field_validator('single_file')
    def single_file_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['read', 'write']):
            raise ValueError("must be one of enum values ('read', 'write')")
        return value

    @field_validator('statuses')
    def statuses_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['read', 'write']):
            raise ValueError("must be one of enum values ('read', 'write')")
        return value

    @field_validator('team_discussions')
    def team_discussions_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['read', 'write']):
            raise ValueError("must be one of enum values ('read', 'write')")
        return value

    @field_validator('vulnerability_alerts')
    def vulnerability_alerts_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['read', 'write']):
            raise ValueError("must be one of enum values ('read', 'write')")
        return value

    @field_validator('workflows')
    def workflows_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['write']):
            raise ValueError("must be one of enum values ('write')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of AppPermissions from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of AppPermissions from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "actions": obj.get("actions"),
            "administration": obj.get("administration"),
            "checks": obj.get("checks"),
            "contents": obj.get("contents"),
            "deployments": obj.get("deployments"),
            "environments": obj.get("environments"),
            "issues": obj.get("issues"),
            "members": obj.get("members"),
            "metadata": obj.get("metadata"),
            "organization_administration": obj.get("organization_administration"),
            "organization_hooks": obj.get("organization_hooks"),
            "organization_packages": obj.get("organization_packages"),
            "organization_plan": obj.get("organization_plan"),
            "organization_projects": obj.get("organization_projects"),
            "organization_secrets": obj.get("organization_secrets"),
            "organization_self_hosted_runners": obj.get("organization_self_hosted_runners"),
            "organization_user_blocking": obj.get("organization_user_blocking"),
            "packages": obj.get("packages"),
            "pages": obj.get("pages"),
            "pull_requests": obj.get("pull_requests"),
            "repository_hooks": obj.get("repository_hooks"),
            "repository_projects": obj.get("repository_projects"),
            "secret_scanning_alerts": obj.get("secret_scanning_alerts"),
            "secrets": obj.get("secrets"),
            "security_events": obj.get("security_events"),
            "single_file": obj.get("single_file"),
            "statuses": obj.get("statuses"),
            "team_discussions": obj.get("team_discussions"),
            "vulnerability_alerts": obj.get("vulnerability_alerts"),
            "workflows": obj.get("workflows")
        })
        return _obj


